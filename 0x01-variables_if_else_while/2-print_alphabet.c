#include <stdio.h>

/**
 * main - beginining of the code
 * @void: Return  value
 * Return: always 0
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
	putchar('\n');

	return (0);
}
